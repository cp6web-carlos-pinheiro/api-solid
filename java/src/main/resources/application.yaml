spring:
  application:
    name: store

  datasource:
    url: jdbc:mysql://localhost:3306/store?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: store
    password: store123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true